{
  "name": "lava-cash",
  "version": "1.0.0",
  "description": "Sistema de gerenciamento financeiro com validação de código via linters",
  "private": true,
  "scripts": {
    "test": "jest --runInBand",
    "lint": "npm run lint:html && npm run lint:css && npm run lint:js",
    "lint:html": "htmlhint \"*.html\"",
    "lint:css": "stylelint \"css/**/*.css\"",
    "lint:js": "eslint \"js/**/*.js\"",
    "lint:fix": "npm run lint:css -- --fix && npm run lint:js -- --fix"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "htmlhint": "^1.1.4",
    "jest": "^29.0.0",
    "stylelint": "^16.2.1",
    "stylelint-config-standard": "^36.0.0"
  },
  "dependencies": {
    "jimp": "^1.6.0"
  }
}
